# API Gateway

This project implements an API Gateway using Spring Cloud Gateway.

## Technologies
- Spring Boot 3.x
- Spring Cloud Gateway
- Eureka Discovery Client
- Spring Security (JWT)
- Maven

## Architecture
The API Gateway acts as a single entry point for all microservices and handles routing, authentication, and service discovery.

## How to Run
1. Start Eureka Server
2. Run API Gateway
