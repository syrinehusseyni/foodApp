name: Food Delivery Microservices CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

# 🔥 IMPORTANT FIX (this solves your GHCR error)
permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: ${{ github.actor }}

jobs:

  # ============================================
  # USER SERVICE
  # ============================================
  user-service:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build JAR
        working-directory: backend/user-service
        run: mvn clean package -DskipTests

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker Image
        run: docker build -t $REGISTRY/$IMAGE_OWNER/user-service:latest backend/user-service

      - name: Push Docker Image
        run: docker push $REGISTRY/$IMAGE_OWNER/user-service:latest


  # ============================================
  # ORDER SERVICE
  # ============================================
  order-service:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build JAR
        working-directory: backend/order-service
        run: mvn clean package -DskipTests

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker Image
        run: docker build -t $REGISTRY/$IMAGE_OWNER/order-service:latest backend/order-service

      - name: Push Docker Image
        run: docker push $REGISTRY/$IMAGE_OWNER/order-service:latest


  # ============================================
  # RESTAURANT SERVICE
  # ============================================
  restaurant-service:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build JAR
        working-directory: backend/restaurant-service
        run: mvn clean package -DskipTests

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker Image
        run: docker build -t $REGISTRY/$IMAGE_OWNER/restaurant-service:latest backend/restaurant-service

      - name: Push Docker Image
        run: docker push $REGISTRY/$IMAGE_OWNER/restaurant-service:latest


  # ============================================
  # ADMIN SERVICE
  # ============================================
  admin-service:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build JAR
        working-directory: backend/admin-service
        run: mvn clean package -DskipTests

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker Image
        run: docker build -t $REGISTRY/$IMAGE_OWNER/admin-service:latest backend/admin-service

      - name: Push Docker Image
        run: docker push $REGISTRY/$IMAGE_OWNER/admin-service:latest


  # ============================================
  # DELIVERY SERVICE
  # ============================================
  delivery-service:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build JAR
        working-directory: backend/delivery-service
        run: mvn clean package -DskipTests

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker Image
        run: docker build -t $REGISTRY/$IMAGE_OWNER/delivery-service:latest backend/delivery-service

      - name: Push Docker Image
        run: docker push $REGISTRY/$IMAGE_OWNER/delivery-service:latest


  # ============================================
  # API GATEWAY
  # ============================================
  api-gateway:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build JAR
        working-directory: backend/api-gateway
        run: mvn clean package -DskipTests

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker Image
        run: docker build -t $REGISTRY/$IMAGE_OWNER/api-gateway:latest backend/api-gateway

      - name: Push Docker Image
        run: docker push $REGISTRY/$IMAGE_OWNER/api-gateway:latest


  # ============================================
  # EUREKA SERVER
  # ============================================
  eureka-server:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build JAR
        working-directory: backend/eureka-server
        run: mvn clean package -DskipTests

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker Image
        run: docker build -t $REGISTRY/$IMAGE_OWNER/eureka-server:latest backend/eureka-server

      - name: Push Docker Image
        run: docker push $REGISTRY/$IMAGE_OWNER/eureka-server:latest


  # ============================================
  # FRONTEND
  # ============================================
  frontend:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: frontend
        run: npm install

      - name: Build frontend
        working-directory: frontend
        run: npm run build